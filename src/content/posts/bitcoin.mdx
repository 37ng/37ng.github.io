---
title: "Bitcoin, and then miners"
description: "Pools, farms and miners"
date: "2026-07-28"
tags: ["bitcoin", "crypto", "cryptocurrency"]
heroImage: "../../assets/bitcoin-bg.webp"
color: "#F7941D"
stageTone: "dark"
---

import BitcoinTimeline from "@/components/diagrams/BitcoinTimeline.astro";
import FlagRateMempool from "@/components/diagrams/FlagRateMempool.astro";
import FlaggedBlocks from "@/components/diagrams/FlaggedBlocks.astro";
import PoolFlagHeatmap from "@/components/diagrams/PoolFlagHeatmap.astro";
import { OffchainRevenue } from "@/components/diagrams/OffchainRevenue";

<BitcoinTimeline variant="post" />

Bitcoin's subsidy havles every 210000 blocks(~4 years). By the time of 33 halving(~year 2140), it will shrink to 0. Then transaction fee will be the only compensation for maintaining the network.

How does transaction fee look like?

## Onchain revenue

First, look at onchain revenue over the years.

(show subsidy + txFee bar chart, diff color)

if we omit subsidy:

(show txFee bar chart)

## offchain revenue

Meanwhile, let's look at offchain revenue. mempool.space provides services publish acceleration data, records accelerated txs via its router. 

I paid $120 to accelerate a tx 8ab712793f52be691bfd70a971d30634168e3b71d0473bccab5f6486062c79be
$120 was worth of ~155000 sats and the fee delta is 30000 sats. The actual fee boost is 0 since the tx has paid above the block's rate.
[We find a formula to convert api data to actual payment]

We crawl all data and group them by month x pools.

(bar chart, each pool uses a diff color)

Template vs Actual blocks are another source, but it's not certain that the differences is due to what factor.

## assumed offchain revenue

There are more offchain revenues, private business deals, un published data, so we try to find the pattern that could be offchain txs by observing mempool.space API.

[same charts, but with addition of assumed offchain revenue]

## Flag rate vs mempool congestion

<FlagRateMempool />

<FlaggedBlocks />

<PoolFlagHeatmap />

The numbers here are placeholder data.

## What a pool earns off the book

<OffchainRevenue client:visible />

